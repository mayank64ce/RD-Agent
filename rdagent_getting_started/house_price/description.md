# Task Description
Predict the house `price` based on features like `square_feet`, `num_bedrooms`, `num_bathrooms`, and `location_score`.
The evaluation metric is RMSE (Root Mean Squared Error).

# Data Description
- `train.csv`: Training dataset containing features and the target variable `price`.
- `test.csv`: Test set with features only; predict the `price` for these entries.
